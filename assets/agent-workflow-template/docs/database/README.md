# Database And Runtime Data

Use this directory for schema, migration, runtime config, data-access, and operational data notes.

Document:

- Tables, collections, indexes, enums, and migrations.
- Runtime configuration and permission groups.
- Query boundaries, partitioning rules, retention, and deletion semantics.
- Backup, restore, and local setup steps.
- High-risk queries and load-review requirements.

TODO(owner): Split this into `schema-overview.md`, `runtime-config.md`, or data-access pattern docs when the project needs more detail.
